#lol

country_event = {
	id = 500073
	title = "removing nuke clouds"
	desc = "just what it says on the tin"

	trigger = {#I dont think this or block works..
		NOT = {has_country_modifier = nuke_remove_timer}
		tag = JAN
	}

	mean_time_to_happen = {
		days = 1
	}

	option = {
		name = "bye!"
		add_country_modifier = { name = nuke_remove_timer duration = 30 }
		any_country = {
			limit = {
				any_owned_province = {
					has_province_flag = disease 
					NOT = {has_province_modifier = nuked_1}
					NOT = {has_province_modifier = nuked_2}
					NOT = {has_province_modifier = nuked_3}
					NOT = {has_province_modifier = nuked_4}
					NOT = {has_province_modifier = nuked_5}
				}
			}
			any_owned = {
				limit = {
					has_province_flag = disease 
					NOT = {has_province_modifier = nuked_1}
					NOT = {has_province_modifier = nuked_2}
					NOT = {has_province_modifier = nuked_3}
					NOT = {has_province_modifier = nuked_4}
					NOT = {has_province_modifier = nuked_5}
				}
				clr_province_flag = disease
			}
		}
	}
}